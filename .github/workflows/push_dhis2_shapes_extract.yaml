name: Push dhis2_shapes_extract pipeline

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/dhis2_shapes_extract.yaml"
      - "dhis2_shapes_extract/**"
      - "!dhis2_shapes_extract/docs/**"
      - "!dhis2_shapes_extract/README.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: '3.11'
      
      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "ds-templates"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push dhis2_shapes_extract \
            --code "dhis2-shapes-extract" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/openhexa-templates-ds/commit/${{ github.sha }}" \
            --yes
